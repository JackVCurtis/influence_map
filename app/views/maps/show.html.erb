<div class="container">
  <div class="row">
    <h2><%= @map.name %></h2>
    <h2><%= @map.user.email %></h2>
  </div>

  <div class="row">
    <div class="col-xs-12">
      <%= render 'map_img' %>
    </div>
  </div>
</div>

<script type="text/javascript">
  var dataCollection = new DataCollection({});
  var mapView = new MapView({collection: dataCollection});
  var formView = new FormView({collection: dataCollection});
</script>